part of 'screens.dart';

class styling_widget extends StatelessWidget {
  const styling_widget({super.key});

  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child: Container(),
    );
  }
}
